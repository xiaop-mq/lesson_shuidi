为什么面试官要问这道题，这不是一道题，可以面出一堆的知识脉络...
代码复用  性能优化(栈，堆)

应用场景： 继承(父类的构造函数继承) 原型链 类型检测 类数组 Math.max.call(Math, arr) 解构

1. istype   借用Object.prototype.trString.call() // 表达完整
2. 判断数组的最大值[15, 6, 12, 13, 16]

浅显的认识 上升到 JS基础扎实的话 对应 call、apply、bind做深入的分析


call, apply 核心是借用方法，
js 基础API，好用
1. 高级变种，出其不意。
2. 为什么出这道题？


常考的考题不再是秘密， 难题 20%  10%

高频考题是否能回答的惊艳，important !

标准答案(记忆力，覆盖度)  能力(上不去，js，逻辑能力，业务) 

标准答案(你认为) + 能力(表达能力，面试官认可的能力，打破砂锅问到底，为什么面试官要面这道题，覆盖这个知识脉络) = 惊艳 

- 手写源码 bind